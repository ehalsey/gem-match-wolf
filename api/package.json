{
  "name": "bejeweled-api",
  "version": "1.0.0",
  "description": "Azure Functions API for Bejeweled high scores",
  "main": "dist/src/functions.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "storage": "azurite --silent --location azurite --debug azurite/debug.log",
    "storage:verbose": "azurite --location azurite --debug azurite/debug.log",
    "start:local": "npm run build && start /B azurite --silent && func start",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:integration": "jest --testMatch='**/*.integration.test.ts'"
  },
  "dependencies": {
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "azurite": "^3.35.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.3.0"
  }
}
